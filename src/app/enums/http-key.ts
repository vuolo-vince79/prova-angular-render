export enum RequestUrl{
    dbUrl = "https://login-pr.up.railway.app"
}

export enum StorageKey{
    accessToken = "access-token",
    refreshToken = "refresh-token",
    username = "username",
    role = "role"
}